# Agentic RAG — Indian IT Financials

Answers natural language questions about Infosys, TCS, and Wipro by routing to the right tool,
composing a grounded answer with citations, and refusing gracefully when it cannot help.

No black-box agent frameworks. Every decision is traceable and explainable line by line.

---

## What it does

- Takes a natural language question
- Generates a pre-loop plan describing which tools it intends to use (Bonus A)
- Decides which of 3 tools to call: document search, financial data, or live web search
- Calls tools in sequence, up to a hard cap of 8 steps
- Composes a cited answer using the LLM, with a self-critique pass (Bonus C)
- Records per-tool latency and estimated cost (Bonus B)
- Caches responses to avoid redundant API calls on repeated questions
- Refuses gracefully for investment advice, out-of-scope questions, and unanswerable queries

---

## Project structure

```
├── agent/
│   ├── __init__.py
│   ├── agent_loop.py       # Core agent loop — read this first
│   ├── cache.py            # File-backed response cache
│   ├── decision_engine.py  # LLM-powered tool routing with rule-based fallback
│   ├── llm.py              # Unified LLM interface (Groq + Gemini with fallback)
│   ├── planner.py          # Pre-loop planning step (Bonus A)
│   ├── reflector.py        # Post-answer self-critique (Bonus C)
│   └── telemetry.py        # Per-tool latency + cost tracking (Bonus B)
├── tools/
│   ├── __init__.py
│   ├── query_data.py       # LLM-driven SQL over financial data (SQLite)
│   ├── search_docs.py      # Semantic search over PDFs (FAISS)
│   └── web_search.py       # Live web search (Tavily)
├── utils/
│   ├── logger.py           # Trace formatter + JSON export
│   └── types.py            # Shared data structures
├── data/
│   ├── docs/               # Annual report PDFs (place PDFs here before running ingest.py)
│   ├── build_db.py         # Builds financials.db + financials.csv from published data
│   ├── chunks_metadata.pkl # FAISS chunk metadata (generated by ingest.py)
│   ├── faiss_index.bin     # FAISS vector index (generated by ingest.py)
│   ├── financials.csv      # Structured financial data FY15–FY24 (generated by build_db.py)
│   └── financials.db       # SQLite DB with annual + quarterly data (generated by build_db.py)
├── traces/
│   ├── response_cache.json # Cached agent responses (auto-created)
│   └── evaluation_results.json  # Eval output (generated by evaluate.py)
├── DESIGN.md               # Agent architecture and tool contracts
├── EVALUATION.md           # Evaluation methodology and failure analysis
├── evaluate.py             # Runs 20-question eval set with Jaccard scoring
├── ingest.py               # PDF → FAISS index builder
├── main.py                 # CLI entry point
├── requirements.txt
├── .env.example            # API key template
└── .gitignore
```

---

## Setup

```bash
# 1. Install dependencies
pip install -r requirements.txt

# 2. Copy and fill in your API keys
cp .env.example .env

# 3. Build the SQLite database (one-time, ~2 seconds)
python data/build_db.py

# 4. Place annual report PDFs in data/docs/ then build the FAISS index (one-time, ~5 mins)
python ingest.py
```

---

## API keys

| Key | Where to get | Required? |
|---|---|---|
| `GEMINI_API_KEY` | [aistudio.google.com](https://aistudio.google.com) | One of Gemini or Groq required |
| `GROQ_API_KEY` | [console.groq.com](https://console.groq.com) | One of Gemini or Groq required |
| `TAVILY_API_KEY` | [tavily.com](https://tavily.com) | Yes (for live web search) |
| `HF_TOKEN` | [huggingface.co/settings/tokens](https://huggingface.co/settings/tokens) | Optional (suppresses HF warning) |

---

## LLM routing

Set `LLM_TYPE` in `.env` to control which provider is tried first:

```
LLM_TYPE=GROQ    # Groq first → Gemini fallback (recommended: higher free-tier RPM)
LLM_TYPE=GEMINI  # Gemini first → Groq fallback
```

If both fail, the agent falls back to rule-based routing — it still works, just with reduced accuracy.

To switch models:
```
GEMINI_MODEL=gemini-2.0-flash-lite   # default (higher free RPM than gemini-2.0-flash)
GROQ_MODEL=llama-3.3-70b-versatile   # default
```

---

## Run the agent

```bash
python main.py
```

Type any question. The agent prints a full trace showing the plan, each tool call with
reasoning, the final answer with citations, a self-critique, and per-tool telemetry.

Repeated questions are served from `traces/response_cache.json` instantly — no API calls.

---

## Run the evaluation set

```bash
python evaluate.py
```

Runs all 20 questions across 4 categories and prints:
- Per-question Jaccard tool-routing score and status
- Per-category accuracy summary
- Per-tool telemetry table (latency, call count, estimated cost)

Results saved to `traces/evaluation_results.json`.

---

## Data sources

**Structured data (`data/financials.db`):**
- FY15–FY24 annual data for Infosys, TCS, Wipro
- Last 12 quarters (FY22–FY24) for all three companies
- Columns: revenue, expenses, operating profit, OPM%, net profit, EPS, headcount
- Source: company annual reports, Screener.in

**Unstructured data (`data/docs/`):**
- TCS Annual Report 2024-2025
- Wipro Integrated Annual Report 2024-2025
- Infosys Annual Report 2024-2025

**Live web search:**
- Powered by Tavily API — returns real-time snippets with URLs and publication dates

---

## Known failure modes

1. **Rule-based NL→SQL fails on complex queries** — when all LLM providers are unavailable, `_rule_based_nl_to_sql` uses keyword matching and cannot handle multi-company aggregations. Mitigation: LLM-driven SQL is the primary path when any API key is set.

2. **LLM over-refuses stock-related questions** — Llama via Groq sometimes treats "Tell me about Infosys stocks" as investment advice. Fixed in the system prompt: only explicit buy/sell/invest advice triggers a refusal.

3. **FAISS retrieval quality on short/ambiguous queries** — queries like "Why why why?" produce low-quality embedding vectors. The mock fallback uses keyword scoring as a safety net.

4. **Multi-tool questions approaching the hard cap** — questions requiring 3 tools + reflection can use 5-6 of the 8 steps. By design — the cap is a safety feature.

5. **LLM rate limits during bulk evaluation** — free-tier APIs have per-minute limits. The cache skips API calls for repeated questions; switch `LLM_TYPE` between Groq and Gemini to spread load.

---

## AI assistance disclosure

This project was built with AI coding assistance. All design decisions, architecture choices,
and the agent loop logic are understood and can be explained line by line.
